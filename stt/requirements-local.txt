# Local AI dependencies — only needed when STT_BACKEND=local or TTS_BACKEND=local
# Install alongside requirements.txt:  pip install -r requirements-local.txt
openai-whisper>=20240930
kokoro-onnx>=0.4.0
# openwakeword is installed via setup.sh with --no-deps to skip tflite-runtime
# (no Python 3.14 wheel exists); onnxruntime covers the only backend used.
tqdm>=4.0.0
