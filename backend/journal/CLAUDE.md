# Journal attachments (`journal_attachments`, `backend/journal/storage.py`, `src/components/JournalAttachments.tsx`)

Audio, video and photos hung off a journal entry, added/renamed/deleted in the entry's edit mode and playable/readable outside it. Files live under `./data/journal/<attachment_id>/` (`JOURNAL_ROOT`), never as blobs, and uploads **stream to disk** (`file.save`, not `read()`) because a phone video is happily several hundred MB. Things to know:

- **Every attachment is held on the device until the server confirms it** — the same promise the recordings below already made, and the reason this section is no longer only about audio. A file picked, pasted, dropped or staged in the composer is written to `photoStore` (`target: 'journal'`) _before_ anything is POSTed, then uploaded by a registered offline mutation that retries, pauses while the backend is unreachable, survives a reload, and is swept up at boot by `resumeStoredPhotos` if the tab died before the mutation reached the persisted cache. `POST /api/journal/<id>/attachments` therefore takes an optional client-supplied `attachmentId` and returns the row it already holds rather than storing a second copy — the same replay contract `/api/journal/recordings` has, and the early return happens before the file is read. The create and its attachments share `JOURNAL_LANE` (a react-query mutation scope) so a queue replayed after a reload cannot send a photo at an entry that has not landed yet. Until this existed the composer uploaded staged files with a bare `fetch`, no retry, holding them nowhere but React state: on a phone with spotty wifi the picture was gone the moment the request failed, while the recording staged beside it came back — because `recordingStore` had been holding it all along.
- **The global screenshot shortcut has a server-owned session** (`journal_screenshot_sessions`, `POST /api/journal/screenshots`). The first capture creates one empty `Screenshots` entry; the second creates a directly-linked calendar event with a real span, and later captures append to the entry and extend the event from the first capture minute through the last. Deleting an individual screenshot deliberately leaves that historical event alone; the user can remove it manually in the unusual case where the remaining record no longer warrants one. Any normal journal insert closes the session through a database trigger, while saving text/title/tags, renaming or adding another attachment, and merging a recording into the entry close it explicitly. Each attachment is named from the listener's offset-bearing local capture time (`YYYY-MM-DD HH:MM:SS`) and stores that instant as `created_at`, rather than inheriting the later upload time. The upload is deliberately one request with a client-minted attachment id: after a lost response, replay returns the stored attachment before reading the file and cannot create another entry, image or event.
- **Paste and drop are first-class, not a convenience.** The capture happens on the whole editor (`JournalAttachments` wraps the title/body fields as its own paste target) because the media is recorded on a phone or iPad, where exporting to Files and picking it back out is the step being deleted. A paste carrying no media falls through to the textarea untouched. The compose box for a _new_ entry stages files in local state until Save, because the entry has no server-side row for them to hang off yet; `submitNewEntry` then mints its id, writes each staged file to the device and queues it behind the create (above). **Both work on the desktop and neither works on iOS** — getting a Voice Memos recording in is unsolved, and [docs/learnings/ios-voice-memo-capture.md](../../docs/learnings/ios-voice-memo-capture.md) records the dead ends (and why iOS Shortcuts is the next thing to try) so they aren't re-walked. An upload with an empty filename is legitimate and must stay supported either way.
- **A recording is held the same way, in its own store.** `recordingStore` stays separate from `photoStore` because audio is captured _over time_ — chunk-by-chunk appends, a finalized flag, a recovery path for a recorder the OS killed mid-sentence — while a file is complete the instant it is picked. A composer clip also carries `entryId` on the stored recording, not just in its upload's variables: the boot sweep finds an orphan by enumerating the store, and one that has forgotten its entry is filed as a bare journal entry of its own beside the words it was recorded with.
- **A recording made in the app is held on the device until the server confirms it.** `POST /api/journal/recordings` takes the entry id _and_ the attachment id from the client, because the phone re-POSTs the same clip on every reconnect until it lands — so a replay must be a no-op (`INSERT OR IGNORE` for the entry, early return on an attachment id we already hold, checked before the file is read). The browser side writes audio to IndexedDB every five seconds while recording, since iOS suspends a backgrounded page and a screen lock used to destroy the whole take. Design record: [docs/learnings/durable-journal-recordings.md](../../docs/learnings/durable-journal-recordings.md).
- **Stopping a clip in a composer is not a save — Send is** (`src/hooks/useClipStage.ts`, the one staging hook the Journal composer, the Journal editor, the food log, the fanfic reader and Ideas capture all drive). Stopping writes the audio to `recordingStore` and puts a chip on screen; no transcription runs, nothing is uploaded, and the Save button is never disabled by any of it. That is what makes **several clips per entry** possible: talk, pause, talk again, add a photo, then send the lot together. Three things follow.
  - **The entry id is minted at the first chunk, not at Send.** `resumeStoredRecordings` sweeps the device at boot and uploads whatever it finds, so a clip that has forgotten its entry is filed as a bare one of its own. Minting early means a composer killed mid-thought still lands its clips in the right place; Send only adds the words and the pictures. `commit` reads each clip's target back off the _stored recording_ rather than off the live one, because the reader walks to the next chapter with W/S while a thought is still being spoken.
  - **`commit` awaits one upload at a time.** The transcripts are appended in the order the uploads land, so a parallel send would make the running order the network's decision. Nobody awaits `commit`, so a clip paused offline holds up only the clips behind it — which are on disk.
  - **A create and its clips race, and the create must not lose.** Minting the id early means `create_recording_entry` can open the entry — empty — before the create carrying the typed words arrives, and a plain `INSERT OR IGNORE` then dropped them silently. `create_journal_entry` now fills in a row that is still empty and leaves one that has text alone, so a genuine replay is still a no-op. `POST /api/ideas/voice` and `POST /api/food` do the same thing for the same reason.
  - **The editor's clips go up when the editor closes, Cancel included.** The entry already exists, so there is nothing for a clip to be "not saved" into, and discarding a recording because the text edit beside it was abandoned is the silent audio loss this whole path exists to refuse. An unwanted clip is thrown away from its own chip.
- **A clip's transcript is appended to the entry, once.** `_transcribe_attachment_bg`'s `into_entry` used to assign `content`/`raw_content`, which is why one clip per entry was the limit; it now appends with a blank line between clips (`_append_entry_text` — the gap is the pause that was taken) and does so **only on that attachment's first successful transcription**, read before the update. That is what makes the per-attachment Transcribe button safe to press again: it refreshes the clip's own text without pasting it into the entry a second time. Polish, titling and the idea delivery are handed the accumulated body and re-run per clip rather than waiting for the last one — it is not knowable there that another clip is coming, and the three share one FIFO worker, so the version enqueued from the last clip is the one that writes last.
- **The same route is how the Ideas tab records.** An extra `ideaId` makes one upload create three things — the entry, its attachment and the idea — and `_transcribe_attachment_bg` writes its one transcription into both the entry body and the idea (`_deliver_idea_transcript`). The entry is an ordinary journal entry afterwards: it keeps the recording when the idea is deleted, and `journal_entries.idea_id` is `ON DELETE SET NULL` so the link goes and nothing else does. See [backend/research/CLAUDE.md](../research/CLAUDE.md).
- **And how the reader's commentary microphone records.** An extra `ficId` (plus a `chapterId`, absent for a PDF fic) makes the same upload write a `journal_entry_fic_refs` row, so the entry is commentary _on a chapter_ from the moment it exists — `_link_recording_fic`. It rides along with the audio rather than following as a `POST /api/fanfic/<id>/journal-link` call for the same reason `ideaId` does: this upload is replayed until it lands, and a separate request is the one step that can be lost. What it would lose is the only thing saying which chapter the entry was about. An unknown fic (or a chapter from a different one) drops the link and keeps the recording — the audio is the irreplaceable half.
- **`kind` is `audio | video | image | file | youtube`, and for an upload mime beats extension** (`storage.resolve_upload`): mp4/webm carry either, so only the upload's mime type separates a voice memo from a clip. With no usable mime an ambiguous extension defaults to video — audio in a `<video>` costs a blank frame, the reverse throws the picture away.
- **Video takes the speech path, not the vision one.** ffmpeg pulls the audio track out of the container, so `_do_attachment_audio` handles it unchanged.
- **`youtube` is a video watched, not a file uploaded** (`backend/journal/youtube_import.py`, `backend/ytdlp.py`). Paste a link with the composer's 🔗 button and the entry is your commentary while the attachment is the thing commented on. Five things separate it from every other kind:
  - **Its bytes are not under `JOURNAL_ROOT`.** A 720p download goes to `<backup_path>/archive/journal/<attachment_id>/` (`backend/journal/archive.py`), a third collection beside Piano's and Study's on the un-backed-up archive drive. The drive is checked **before** the metadata pass, because failing after 300 MB has landed on the system SSD is the failure that looks like success. The **poster stays on the SSD** at `thumb_path` so the card still draws with the drive unplugged — only playback needs it. `_resolve_attachment_path` tries both roots; each independently requires the path to still be a grandchild of its own root, so a tampered `path` resolves under neither.
  - **The download is a plain daemon thread, not an `llm_jobs` row.** The queue has one worker shared with every caption and polish, and half an hour of yt-dlp in it would head-of-line block all of them. Nothing about the download touches a model; the model work is enqueued at the end. `import_status` is what survives a restart, and `_reset_stale_journal_youtube_imports` un-sticks a row whose thread died.
  - **`import_status` is deliberately separate from `transcript_status`.** The latter means "is a model reading this" and is what `_attachments_settled` gates the metadata wait on — a youtube row lands `idle`, so a long download cannot hold the entry's title for the full 300s cap and then time out anyway. That is also why nothing re-runs metadata when the summary arrives hours later: `_generate_metadata_bg` overwrites title and tags unconditionally, and would clobber a title the user had since set.
  - **Captions first, Whisper second.** yt-dlp fetches subtitles in the _same_ invocation as the video (a second run can disagree with the first about which tracks exist), and `backend/journal/vtt.py` turns them into text. The work there is de-duplication: YouTube's auto-captions are a rolling window, so every line appears twice and a naive join doubles what the summarizer reads. With no captions the downloaded mp4 goes through the ordinary `journal.transcribe_attachment` job. **`into_entry` stays false either way** — the video's words are not the user's words.
  - **The summary is enqueued by whichever branch produced the transcript**, never beside the transcription: the two share one FIFO worker, and queued together the summarizer would run first against an empty transcript. It lands on `description` (`backend/ai/youtube.py`), joins `_attachment_polish_context`, and is the one AI block here that renders **expanded** — every other describes something the reader can already see, while this is the only account of twenty minutes they may not rewatch.
- The user-given `name` is the point (an attachment is "what this is about", not a filename). **Speech transcription is opt-in per attachment** — it costs a real CPU transcription here, and a voice memo is often kept as audio — while image captioning and non-speech audio description fire on upload when their model is configured (`_store_attachment`). All of them run on the `llm_jobs` queue, report through `transcript_status`/`description_status`, and push results over the existing `/api/journal/events` SSE stream.
- **All three blocks of AI text under an attachment collapse once read** (`CollapsibleText`, keyed per attachment _and per kind_ in `localStorage`): open the first time they are seen, closed on every later load. One clip carries both a transcript and a description, so a shared key would make opening either close the other. The speech transcript was the last holdout — reliably the longest of the three and the only one that rendered fully expanded forever.
- Photo captioning and audio description both need the **Multimodal input** checkbox (Settings → llama.cpp), which writes the one `gemma4-12b-omni` alias into both `llama_vision_model` and `llama_audio_model`. The chat preset sets `mmproj-auto = false` and takes text only, so both go to that separate CPU-only any-to-any model — a ~7.4 GB download, which is why it ships **off** and says so. It stayed off longer than intended: the checkbox used to write `gemma4-vision`, a preset that was never defined, so captioning had never once worked — see `backend/ai/images.py`.
