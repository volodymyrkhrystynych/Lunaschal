# Chat (`src/components/Chat/`)

Frontend for the Chat tab's delegate, proposal cards, chat photos, memory writes, and transcript correction — see `backend/delegate/CLAUDE.md` for the full backend contract (SSE frame shapes, proposal lifecycle, `AgentSteps`/`ReasoningBlock` rationale, chat-photo paths). That file covers both sides; this one exists so it also loads when you're only touching files under `src/components/Chat/`.
